const initialState = {
  sent: [],
  received: []
};

const messagingReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default messagingReducer;
